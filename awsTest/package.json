{
  "name": "aws-test",
  "version": "1.0.0",
  "description": "AWS SAM User Management API",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "mysql2": "^3.6.0",
    "ioredis": "^5.3.2"
  },
  "devDependencies": {
    "aws-sdk": "^2.1450.0"
  }
} 
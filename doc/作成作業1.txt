AWSを使用してバックエンドを構築します。
このフォルダにSAMを使用してリポジトリを構築します。
Docker
SAM
Gateway
Lambda
Redis
MySQL
を作ります。ソースコードは以下のような構成いきたい

doc/
awsTest/
├── template.yaml              ← SAM構成
├── lambda/                    ← Lambda関数群（Controller相当）
│   ├── getUser.js
│   └── createUser.js
├── models/                    ← Model層（RDS/Redis接続）
│   ├── userModel.js
│   └── sessionModel.js
├── utils/                     ← 共通ユーティリティ
│   └── db.js                  ← MySQL接続
│   └── redisClient.js        ← Redisクライアント
├── events/                    ← テストイベントJSON
├── docker-compose.yml         ← ローカル開発用Redis/MySQL
└── README.md

ウィンドウズ環境でDocker Desktopを立ち上げてからの作業になりますが
このスレッドで起動とAPI呼び出しとRedisテストまでできれば良いなと考えてます。
最終的にAWSに移行するときの手順を含めたReadmeにしてほしい。

